# Windows 전용 프로젝트. 텍스트 파일은 LF 로 통일한다.
* text=auto eol=lf

# cmd.exe 는 LF 배치 파일에서 오동작하므로 예외
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리
*.png binary
*.jpg binary
*.dds binary
*.hdr binary
*.fbx binary
*.wav binary
*.ttf binary
